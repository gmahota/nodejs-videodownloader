{
  "license": "MIT",
  "dependencies": {
    
  },
  "scripts": {
    "dev": "cd server && yarn dev",
    "start": "cd server && node index.js",
    "heroku-prebuild": "cd server && npm install && npm run postinstall",
    "build": "cd server && npm install && npm run build",
    "web-dev": "cd web && yarn dev"
  }
}
